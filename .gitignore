node_modules/
.next/
.open-next/
out/
build/

# Secrets. Drop is a public repo — nothing here may ever be committed.
.env
.env.local
.env*.local
.dev.vars

.DS_Store
*.tsbuildinfo
next-env.d.ts
coverage/

# Wrangler/miniflare local state
.wrangler/

# Local development database
.localdb/

# Generated by `wrangler types` — 572K of runtime declarations. lib/storage.ts
# declares the small R2 surface Drop actually uses, so a fresh clone type-checks
# without it.
worker-configuration.d.ts

# Built product asset — regenerate with `pnpm asset:build`.
.assets/
